name: Conformance

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: conformance-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  POWERSHELL_TELEMETRY_OPTOUT: '1'
  DOTNET_CLI_TELEMETRY_OPTOUT: '1'
  DOTNET_NOLOGO: '1'

jobs:
  conformance:
    name: Conformance (2026-07-28)
    runs-on: ubuntu-latest
    steps:
      - name: Check out
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Install PowerShell 7.4
        uses: ./.github/actions/install-pwsh
        with:
          version: '7.4.20'
      - name: Node.js version
        run: node --version && npm --version
      - name: Run the conformance suite (server and client legs)
        shell: pwsh
        run: ./build.ps1 -Bootstrap -Task Conformance
      - name: Upload conformance results
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: conformance-results
          path: output/conformance/
          if-no-files-found: warn
